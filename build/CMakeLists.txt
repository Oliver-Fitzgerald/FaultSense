# Project
cmake_minimum_required(VERSION 4.1.1)
project(FaultSense)

# Packages

# Compilation
add_executable(fault-sense )

# Linking
target_link_libraries(fault-sense)

# Including Header Files
target_include_directories(fault-sense)
